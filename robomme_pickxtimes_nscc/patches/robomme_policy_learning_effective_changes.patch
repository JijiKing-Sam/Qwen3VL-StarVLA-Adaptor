diff --git a/examples/robomme/eval.py b/examples/robomme/eval.py
index 245f305..48aac29 100644
--- a/examples/robomme/eval.py
+++ b/examples/robomme/eval.py
@@ -47,6 +47,7 @@ class Args:
     re_eval_tasks: str = "" # tasks split by comma
     only_tasks: str = "" # tasks split by comma
     exclude_tasks: str = "" # tasks split by comma
+    max_episodes_per_task: int = 0 # 0 means all episodes
 
     # VLM subgoal predictor
     use_oracle: bool = False
@@ -308,6 +309,8 @@ def evaluate(args: Args):
 
             env_runner = EnvRunner(task_name, video_save_dir, max_steps=args.max_steps)
             num_episodes = env_runner.num_episodes
+            if args.max_episodes_per_task and args.max_episodes_per_task > 0:
+                num_episodes = min(num_episodes, args.max_episodes_per_task)
 
             success_flag = "unknown"
 
diff --git a/examples/robomme/subgoal_predictor.py b/examples/robomme/subgoal_predictor.py
index 7fa8df4..357e224 100644
--- a/examples/robomme/subgoal_predictor.py
+++ b/examples/robomme/subgoal_predictor.py
@@ -6,16 +6,6 @@ import shutil
 from env_runner import EnvRunner
 from utils import EpisodeState, SUBGOAL_TYPES, TASK_WITH_VIDEO_DEMO
 
-from subgoal_prediction.gemini.api import GeminiModel
-from subgoal_prediction.gemini.prompts import (
-    DEMO_TEXT_QUERY,
-    IMAGE_TEXT_QUERY,
-    VIDEO_TEXT_QUERY,
-)
-
-from subgoal_prediction.qwenvl.api import Qwen3VLModel
-from subgoal_prediction.qwenvl.api_memer import Qwen3VLModelMemER
-
 
 LONG_FIRST_ACTION_TASKS = [
     "BinFill",
@@ -85,6 +75,16 @@ class NullSubgoalPredictor(SubgoalPredictorBase):
 
 class GeminiSubgoalPredictor(SubgoalPredictorBase):
     def start_episode(self, epstate: EpisodeState, env_runner: EnvRunner) -> None:
+        from subgoal_prediction.gemini.api import GeminiModel
+        from subgoal_prediction.gemini.prompts import (
+            DEMO_TEXT_QUERY,
+            IMAGE_TEXT_QUERY,
+            VIDEO_TEXT_QUERY,
+        )
+
+        self._demo_text_query = DEMO_TEXT_QUERY
+        self._image_text_query = IMAGE_TEXT_QUERY
+        self._video_text_query = VIDEO_TEXT_QUERY
         super().start_episode(epstate, env_runner)
         self.api = GeminiModel(
             save_dir=os.path.join(self.save_dir, self.env_name, f"ep{self.episode_id}"),
@@ -137,11 +137,11 @@ class GeminiSubgoalPredictor(SubgoalPredictorBase):
     def _get_text_query(self, count: int) -> str:
         if count == 0:
             if self.env_name in TASK_WITH_VIDEO_DEMO:
-                template = DEMO_TEXT_QUERY
+                template = self._demo_text_query
             else:
-                template = IMAGE_TEXT_QUERY
+                template = self._image_text_query
         else:
-            template = VIDEO_TEXT_QUERY
+            template = self._video_text_query
         return template.format(task_goal=self.task_goal)
 
     def _should_call(self, count: int) -> bool:
@@ -155,6 +155,8 @@ class GeminiSubgoalPredictor(SubgoalPredictorBase):
 class QwenVLSubgoalPredictor(SubgoalPredictorBase):
     
     def setup_api(self) -> None:
+        from subgoal_prediction.qwenvl.api import Qwen3VLModel
+
         self.api = Qwen3VLModel(
             adapter_path=self.args.qwenvl_simpleSG_adapter_path if self.args.subgoal_type == "simple_subgoal" else self.args.qwenvl_groundSG_adapter_path,
             subgoal_type=self.args.subgoal_type,
@@ -200,6 +202,8 @@ class QwenVLSubgoalPredictor(SubgoalPredictorBase):
 
 class MemERSubgoalPredictor(SubgoalPredictorBase):
     def setup_api(self) -> None:
+        from subgoal_prediction.qwenvl.api_memer import Qwen3VLModelMemER
+
         self.api = Qwen3VLModelMemER(adapter_path=self.args.memer_adapter_path)
         print("[robomme] MemER agent setup finished")
     
@@ -258,4 +262,3 @@ def build_subgoal_predictor(
     return NullSubgoalPredictor(args, save_dir)
 
 
-
diff --git a/scripts/train.py b/scripts/train.py
index a8be7a5..6cc9d0c 100644
--- a/scripts/train.py
+++ b/scripts/train.py
@@ -8,6 +8,7 @@ import numpy as np
 import optax
 import wandb
 import time
+import os
 
 from typing import Any
 import tqdm_loggable.auto as tqdm
@@ -82,7 +83,7 @@ def init_wandb(
             name=config.exp_name,
             config=dataclasses.asdict(config),
             project=config.project_name,
-            entity="daiyp_umich",
+            entity=os.getenv("WANDB_ENTITY") or None,
         )
         (ckpt_dir / "wandb_id.txt").write_text(wandb.run.id)
 
@@ -454,6 +455,10 @@ def main(config: _config.TrainConfig, tentative_run: bool = False):
 
 
 if __name__ == "__main__":
-    main(_config.cli(), tentative_run=True)
+    config = _config.cli()
+    # The tentative warmup run is only for JAX compilation. Keeping it off W&B
+    # prevents duplicate step-0 points and active-run reuse in the real run.
+    main(dataclasses.replace(config, wandb_enabled=False), tentative_run=True)
+    wandb.finish()
     time.sleep(20)
-    main(_config.cli())
+    main(config)
diff --git a/src/mme_vla_suite/policies/policy_config.py b/src/mme_vla_suite/policies/policy_config.py
index f081c57..c11af44 100644
--- a/src/mme_vla_suite/policies/policy_config.py
+++ b/src/mme_vla_suite/policies/policy_config.py
@@ -33,7 +33,7 @@ def create_trained_policy(
     history_config_path = checkpoint_dir.parent / "history_config.txt"
     if history_config_path.exists():
         with open(history_config_path, "r") as f:
-            history_config = f.read()
+            history_config = f.read().strip()
     
     if train_config.model.history_config != history_config:
         print(f" == You are using {train_config.model.history_config}, changing to {history_config} ==")
diff --git a/src/mme_vla_suite/training/config.py b/src/mme_vla_suite/training/config.py
index 254c130..f7bd5f0 100644
--- a/src/mme_vla_suite/training/config.py
+++ b/src/mme_vla_suite/training/config.py
@@ -16,6 +16,7 @@ import openpi.models.model as _model
 import openpi.models.pi0_config as pi0_config
 import openpi.models.tokenizer as _tokenizer
 import openpi.shared.download as _download
+import openpi.shared.nnx_utils as nnx_utils
 import openpi.shared.normalize as _normalize
 import openpi.training.droid_rlds_dataset as droid_rlds_dataset
 import openpi.training.optimizer as _optimizer
@@ -618,6 +619,117 @@ _CONFIGS = [
         ema_decay=0.999,
         fsdp_devices=4,
     ),
+    TrainConfig(
+        name="mme_vla_suite_mem_only",
+        model=history_pi0.HistoryPi0Config(
+            pi05=True,
+            action_horizon=20,
+            use_history=True,
+            history_config=None,
+            discrete_state_input=False,
+        ),
+        data=RoboMMEDataConfig(
+            repo_id=f"robomme",
+            base_config=DataConfig(prompt_from_task=True),
+        ),
+        batch_size=8,
+        lr_schedule=_optimizer.CosineDecaySchedule(
+            warmup_steps=1_000,
+            peak_lr=5e-5,
+            decay_steps=20_000,
+            decay_lr=5e-5,
+        ),
+        optimizer=_optimizer.AdamW(clip_gradient_norm=1.0),
+        # Full MME-VLA finetuning OOMs on a 40GB A100 while creating
+        # optimizer state, so this specialist config trains only memory params.
+        freeze_filter=nnx.Not(nnx_utils.PathRegex(".*mem.*")),
+        weight_loader=weight_loaders.CheckpointWeightLoader(
+            os.getenv(
+                "MMEVLA_INIT_PARAMS",
+                os.path.join(OPENPI_DATA_HOME, "openpi-assets/checkpoints/pi05_base/params"),
+            ),
+        ),
+        num_train_steps=10_000,
+        save_interval=1_000,
+        keep_period=1_000,
+        num_workers=2,
+        ema_decay=0.999,
+        fsdp_devices=1,
+    ),
+    TrainConfig(
+        name="mme_vla_suite_mem_action",
+        model=history_pi0.HistoryPi0Config(
+            pi05=True,
+            action_horizon=20,
+            use_history=True,
+            history_config=None,
+            discrete_state_input=False,
+        ),
+        data=RoboMMEDataConfig(
+            repo_id=f"robomme",
+            base_config=DataConfig(prompt_from_task=True),
+        ),
+        batch_size=8,
+        lr_schedule=_optimizer.CosineDecaySchedule(
+            warmup_steps=1_000,
+            peak_lr=5e-5,
+            decay_steps=20_000,
+            decay_lr=5e-5,
+        ),
+        optimizer=_optimizer.AdamW(clip_gradient_norm=1.0),
+        # Slightly larger specialist: keep the giant VLM frozen, but allow
+        # memory plus action/time adapters to move together.
+        freeze_filter=nnx.Not(nnx_utils.PathRegex(".*(mem|action|time).*")),
+        weight_loader=weight_loaders.CheckpointWeightLoader(
+            os.getenv(
+                "MMEVLA_INIT_PARAMS",
+                os.path.join(OPENPI_DATA_HOME, "openpi-assets/checkpoints/pi05_base/params"),
+            ),
+        ),
+        num_train_steps=10_000,
+        save_interval=1_000,
+        keep_period=1_000,
+        num_workers=2,
+        ema_decay=0.999,
+        fsdp_devices=1,
+    ),
+    TrainConfig(
+        name="mme_vla_suite_mem_action_lr2e5",
+        model=history_pi0.HistoryPi0Config(
+            pi05=True,
+            action_horizon=20,
+            use_history=True,
+            history_config=None,
+            discrete_state_input=False,
+        ),
+        data=RoboMMEDataConfig(
+            repo_id=f"robomme",
+            base_config=DataConfig(prompt_from_task=True),
+        ),
+        batch_size=8,
+        lr_schedule=_optimizer.CosineDecaySchedule(
+            warmup_steps=1_000,
+            peak_lr=2e-5,
+            decay_steps=20_000,
+            decay_lr=2e-5,
+        ),
+        optimizer=_optimizer.AdamW(clip_gradient_norm=1.0),
+        # Conservative specialist for quick per-task adaptation when the
+        # standard 5e-5 route starts to hurt the official policy.
+        freeze_filter=nnx.Not(nnx_utils.PathRegex(".*(mem|action|time).*")),
+        weight_loader=weight_loaders.CheckpointWeightLoader(
+            os.getenv(
+                "MMEVLA_INIT_PARAMS",
+                os.path.join(OPENPI_DATA_HOME, "openpi-assets/checkpoints/pi05_base/params"),
+            ),
+        ),
+        num_train_steps=10_000,
+        save_interval=1_000,
+        keep_period=1_000,
+        num_workers=2,
+        ema_decay=0.999,
+        fsdp_devices=1,
+    ),
 ]
 
 if len({config.name for config in _CONFIGS}) != len(_CONFIGS):
